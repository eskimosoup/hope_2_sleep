
  <% if product.image? %>
    <%= link_to image_tag(product.image.url(:highlight), :class => "framed"), product, :title => product.image_alt  %>
    <br />
  <% end %>
  <%= link_to h(product.name), product %>
  <hr />
  <p align="right"><%= link_to "More", product, :class => "more" %></p>

